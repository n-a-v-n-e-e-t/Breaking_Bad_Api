import React from 'react'
import spin from  '../../img/spinner.gif'
const Spinner = () => {
    return (
        <img src={spin} alt="Loading..." className="center" id="spin"/>
    )
}

export default Spinner
